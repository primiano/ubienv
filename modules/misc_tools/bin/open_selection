#!/bin/bash
SEL=$(xsel)
FILE=$(echo $SEL | cut -d':' -f1)
LINE=$(echo $SEL | cut -d':' -f2)

if [ -f "$FILE" ]; then
  if [[ "$LINE" = *[[:digit:]]* ]]; then
    gvim -R "$FILE" +$LINE
  else
    gvim -R "$FILE"
  fi
fi


